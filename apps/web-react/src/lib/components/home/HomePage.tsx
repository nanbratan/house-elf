/**
 * Placeholder. house-elf-shi.10 replaces this with the real app shell; it exists so
 * the scaffold has something to render, type-check, test and prove the build against.
 */
export function HomePage() {
	return (
		<main className="flex min-h-screen flex-col items-center justify-center gap-2">
			<h1 className="text-2xl font-semibold">house-elf</h1>
			<p className="text-muted-foreground">TanStack Start scaffold.</p>
		</main>
	);
}
