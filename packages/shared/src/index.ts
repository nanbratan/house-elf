/**
 * Code shared between the Mastra server and the SvelteKit app.
 *
 * Deliberately empty. The package exists so the workspace, TypeScript project
 * references and test runner wiring are proven end to end, but nothing is shared
 * yet — M2 is the first milestone with a real cross-app consumer. Inventing a
 * function here to have something to test would mean testing guesses.
 */
export {};
