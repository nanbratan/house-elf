# Copy to .env and fill in. .env is git-ignored; this file is not.
# Keep this file current — every variable the app reads should appear here.
#
# Scope: these are read by the Mastra server and by the integration tests. The
# TanStack Start app deliberately does NOT read this file — it is a thin proxy to the
# Mastra server and has no business holding the database URL or the provider key.
# When it needs a variable of its own it gets apps/web/.env, documented in
# apps/web/.env.example.

# --- Postgres -----------------------------------------------------------------
# Dev database. Matches infra/docker-compose.yml (service: postgres, port 5432).
DATABASE_URL=postgresql://houseelf:houseelf@localhost:5432/houseelf

# Integration-test database. Matches infra/docker-compose.yml
# (service: postgres-test, port 5433). Data is tmpfs-backed and disposable.
TEST_DATABASE_URL=postgresql://houseelf:houseelf@localhost:5433/houseelf_test

# --- Model provider -----------------------------------------------------------
# Every model is reached through OpenRouter, which fronts every provider behind
# one key — so billing moves here too: usage is paid from OpenRouter credits,
# not from a provider account of your own. An Anthropic key buys nothing now.
# The key is also needed to read the model catalog: the picker is driven by a
# live fetch of /api/v1/models/user, which is account-scoped and requires auth.
# https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=

# Models are not configured here. Every request names one, validated against the
# allowlist in apps/server/src/mastra/models.ts.
