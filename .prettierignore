node_modules
.svelte-kit
.mastra
build
dist
bun.lock
.env

# Vendored agent skills — third-party content, kept byte-identical to upstream.
.agents
skills-lock.json

# The ai-elements catalogue, snapshotted from its registry as reference material.
# Reformatting it would make the next `diff` against upstream unreadable.
vendor

# Same rule, other vendors: the Beads plugin manifest ships from upstream, and
# Serena rewrites its own config. Neither is ours to reformat.
.copilot-plugin
.serena

# Tool-owned state and generated agent config. `bd`, Claude Code and Codex each
# rewrite these on their own schedule; formatting them is a fight we would lose
# every time one of them regenerates.
.beads
.claude
.codex

# Generated by the TanStack Router plugin from the files in src/routes.
apps/web-react/src/routeTree.gen.ts
